<nav class="navbar navbar-expand-lg bg-white" data-bs-theme="white">
	<div class="container-fluid">
		<a class="navbar-brand" href="{{ path('homepage') }}">Commerce</a>
		<button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarColor02" aria-controls="navbarColor02" aria-expanded="false" aria-label="Toggle navigation">
			<span class="navbar-toggler-icon"></span>
		</button>
		<div class="collapse navbar-collapse" id="navbarColor02">
			<ul class="navbar-nav me-auto">
				<li class="nav-item">
					<a class="nav-link active" href="{{ path('homepage')}}">Home
						<span class="visually-hidden">(current)</span>
					</a>
				</li>
				{% if app.user %}
				<li class="nav-item">
					<a class="nav-link" href="{{ path('product_create') }}"> <i class="fa-solid fa-square-plus" style="color: #74C0FC;"></i> Creation d'un produit</a>
				</li>
				<li class="nav-item">
					<a class="nav-link" href="{{ path('category_create') }}"><i class="fa-solid fa-square-plus" style="color: #74C0FC;"></i> Creation d'une catégorie</a>
				</li>
				{% endif %}




			</ul>
			<ul class="navbar-nav ">
				{% if app.user %}

				<a href="{{ path('cart_show') }}" class="btn btn-outline-dark mx-2">
						<i class="fa-solid fa-cart-shopping fa-sm" style="color: #131313;"></i>
						Mon panier
						<span class="badge bg-dark text-white ms-1 rounded-pill">{{ cartItem.nbrItems }}</span>
				</a>
{#					<li class="nav-items"><a href="{{ path('cart_show') }}" class="btn btn-success">Mon panier </a></li>#}
				<li class="nav-items"><a href="{{ path('app_logout') }}" class="btn btn-danger">logout</a></li>
				{% else %}
					<li class="nav-items"><a href="#" class="nav-link">Inscription</a></li>
					<li class="nav-items"><a href="{{ path('app_login') }}" class="btn btn-success">Login</a></li>
				{% endif %}
			</ul>
		</div>
	</div>
</nav>